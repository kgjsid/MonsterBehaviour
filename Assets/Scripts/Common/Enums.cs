public enum eState
{
    Idle,
    Trace,
    Attack,
    Hit,
    Die,
    End
}

public enum eNodeStatus
{
    Success,
    Failure,
    Running
}

public enum eMonsterID
{
    Slime,
    Orc,
    End
}